# 🚀 Инструкции по настройке чат-приложения Exa & Deepseek

## ✅ Исправленные проблемы
1. **Удалена конфигурация basePath** - Приложение было настроено с basePath `/deepseekchat`, что вызывало проблемы с маршрутизацией в локальной разработке
2. **Создан файл .env.local** - Отсутствовал файл с переменными окружения
3. **API эндпоинты теперь доступны** - Маршруты работают корректно без basePath
4. **Интерфейс переведен на русский язык** - Все текстовые элементы теперь на русском

## 🔑 Настройка API ключей

### Шаг 1: Получите ваши API ключи
1. **API ключ Exa**: Перейдите на [Exa Dashboard](https://dashboard.exa.ai/api-keys) и создайте API ключ
2. **API ключ Fireworks AI**: Перейдите на [Fireworks Dashboard](https://app.fireworks.ai/api-keys) и создайте API ключ

### Шаг 2: Настройте переменные окружения
Отредактируйте файл `.env.local` в корне проекта и замените значения-заполнители:

```env
EXA_API_KEY=ваш_настоящий_exa_api_ключ
FIREWORKS_API_KEY=ваш_настоящий_fireworks_api_ключ
```

### Шаг 3: Перезапустите сервер разработки
```bash
npm run dev
```

## 🌐 Доступ к приложению
- Откройте браузер и перейдите на: `http://localhost:3001`
- Приложение теперь должно работать правильно с функциями веб-поиска и AI чата на русском языке

## 🧪 Тестирование API
Вы можете протестировать API эндпоинты напрямую:

### Тестирование API веб-поиска
```bash
curl -X POST http://localhost:3001/api/exawebsearch \
  -H "Content-Type: application/json" \
  -d '{"query":"последние новости об AI"}'
```

### Тестирование API чата
```bash
curl -X POST http://localhost:3001/api/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"Здравствуйте"}]}'
```

## 🐛 Устранение неполадок
- **Ошибки 404**: Убедитесь, что сервер разработки запущен и вы используете правильный порт (3001)
- **Ошибки API ключей**: Проверьте, что ваши API ключи правильно установлены в `.env.local`
- **Поиск не работает**: Убедитесь, что ваш API ключ Exa действителен и имеет кредиты
- **Чат не работает**: Убедитесь, что ваш API ключ Fireworks действителен и имеет доступ к моделям Deepseek

## 📋 Структура проекта
- `app/api/chat/` - Эндпоинт чата с использованием Deepseek через Fireworks
- `app/api/exawebsearch/` - Эндпоинт веб-поиска с использованием API Exa
- `app/page.tsx` - Основной интерфейс чата (переведен на русский)
- `app/layout.tsx` - Метаданные и заголовки (переведены на русский)
- `.env.local` - Переменные окружения (API ключи)

Приложение теперь полностью настроено и будет работать на русском языке!
